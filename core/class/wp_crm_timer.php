<?php
class WP_CRM_Timer extends WP_CRM_Model {
	/*
	$object -> check the stamp of the object then compare it (with a finite, pre-defined set of operations) to the current server timestamp.
	if the object is a timer, check its stamp against current stamp, then update the stamp.
	*/
	public static function install () {
		}
	}
?>
